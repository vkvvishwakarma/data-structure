function mergeSort(){

    //merge(L , R)
    // [2,56],[9,10]
    //initialize the empty array
   // compare the first index's element of  left array to the  first index's element of right array
    // push lower values to empty array
   // shift the array with the lower value
   // repeate untill both arrays are empty


}